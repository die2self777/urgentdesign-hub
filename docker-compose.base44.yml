services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    command: sh -c "npm install && npx nodemon server.js"
    env_file:
      - ./.env.base44-defaults
      - /run/base44/app.env
    restart: unless-stopped
